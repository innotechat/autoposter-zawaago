import { useMemo, useRef, useState } from "react";
import { ArrowLeft, CheckCircle, FilmStrip, Play, Sparkle, SpinnerGap, SpeakerHigh, WarningCircle } from "@phosphor-icons/react";

type Scene = { scene: number; durationSeconds: number; narration: string; caption: string; visualPrompt: string; imageUrl?: string };
type Storyboard = { title: string; hook: string; totalSeconds: number; scenes: Scene[]; pageName: string; language: string; audience: string };
const LOGOS: Record<string, string> = { Zawaago: "/brand/zawaago-logo-final.svg", InnoTech: "/brand/innotech-logo-final.svg" };
const TOPIC = "AI Agent vs Chatbot — what is the real difference?";

function wrapText(ctx: CanvasRenderingContext2D, text: string, maxWidth: number) { const words = text.split(/\s+/); const lines: string[] = []; let line = ""; for (const word of words) { const next = line ? `${line} ${word}` : word; if (ctx.measureText(next).width > maxWidth && line) { lines.push(line); line = word; } else line = next; } if (line) lines.push(line); return lines; }
function drawCover(ctx: CanvasRenderingContext2D, image: HTMLImageElement, width: number, height: number, progress: number) { const ratio = image.naturalWidth / image.naturalHeight; const target = width / height; let sw = image.naturalWidth; let sh = image.naturalHeight; if (ratio > target) sw = sh * target; else sh = sw / target; const sx = (image.naturalWidth - sw) / 2; const sy = (image.naturalHeight - sh) / 2; const zoom = 1.04 + Math.sin(progress * Math.PI) * 0.035; const dw = width * zoom; const dh = height * zoom; ctx.drawImage(image, sx, sy, sw, sh, (width - dw) / 2, (height - dh) / 2, dw, dh); }
async function loadImage(src: string) { const image = new Image(); image.decoding = "async"; image.src = src; await image.decode(); return image; }
function drawBrandMark(ctx: CanvasRenderingContext2D, logo: HTMLImageElement, width: number) { const logoW = Math.round(width * 0.22); const logoH = logoW * (logo.naturalHeight / logo.naturalWidth); const pad = Math.max(8, Math.round(width * 0.018)); const badgeW = logoW + pad * 2; const badgeH = logoH + pad * 2; const x = width - badgeW - 22; const y = 22; ctx.save(); ctx.shadowColor = "rgba(0,0,0,.22)"; ctx.shadowBlur = 14; ctx.shadowOffsetY = 3; ctx.fillStyle = "rgba(255,255,255,.92)"; ctx.beginPath(); ctx.roundRect(x, y, badgeW, badgeH, Math.round(pad * 1.35)); ctx.fill(); ctx.restore(); ctx.drawImage(logo, x + pad, y + pad, logoW, logoH); }

export default function ReelLab() {
  const [pageName, setPageName] = useState<"Zawaago" | "InnoTech">("Zawaago");
  const [language, setLanguage] = useState("Hinglish");
  const [storyboard, setStoryboard] = useState<Storyboard | null>(null);
  const [busy, setBusy] = useState<"storyboard" | "assets" | "render" | "save" | "publish" | "schedule" | null>(null);
  const [status, setStatus] = useState(""); const [error, setError] = useState("");
  const [videoUrl, setVideoUrl] = useState(""); const [videoMime, setVideoMime] = useState(""); const [savedVideoUrl, setSavedVideoUrl] = useState("");
  const [scheduleAt, setScheduleAt] = useState(""); const [scheduled, setScheduled] = useState(false); const canvasRef = useRef<HTMLCanvasElement>(null);
  const total = useMemo(() => storyboard?.scenes.reduce((sum, scene) => sum + scene.durationSeconds, 0) || 0, [storyboard]);

  async function createStoryboard() { setBusy("storyboard"); setError(""); setVideoUrl(""); setSavedVideoUrl(""); setScheduled(false); setStatus("Designing the five-scene Reel story…"); try { const response = await fetch("/api/reel-lab/storyboard", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pageName, language, topic: TOPIC, audience: "Business Owners and Professionals" }) }); const data: any = await response.json(); if (!response.ok) throw new Error(data.details || data.error || "Storyboard generation failed"); setStoryboard(data as Storyboard); setStatus("Storyboard ready. Generate the five scene visuals next."); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }

  async function generateSceneAssets() { if (!storyboard) return; setBusy("assets"); setError(""); setVideoUrl(""); setSavedVideoUrl(""); setScheduled(false); try { const scenes: Scene[] = []; for (let i = 0; i < storyboard.scenes.length; i += 1) { const scene = storyboard.scenes[i]; setStatus(`Generating scene visual ${i + 1}/${storyboard.scenes.length}…`); const response = await fetch("/api/autoposter/generate-image", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pageName, topic: `${storyboard.title} · scene ${scene.scene}`, contentType: "Educational Reel Scene", tone: language === "Hinglish" ? "Professional Hinglish" : "Professional English", language, audience: "Business Owners and Professionals", visualStyle: "Premium Editorial Cinematic", aspectRatio: "Portrait 9:16", branding: "No branding", logoPosition: "Bottom Right", cta: "None", customPrompt: `${scene.visualPrompt} Clean artwork only. No text, logos, letters, numbers, subtitles or watermarks. Designed for a vertical 9:16 educational Reel.`, imgPrompt: scene.visualPrompt }) }); const data: any = await response.json(); if (!response.ok || !data.imageUrl) throw new Error(data.details || data.error || `Scene ${i + 1} image generation failed`); scenes.push({ ...scene, imageUrl: data.imageUrl }); setStoryboard((current) => current ? { ...current, scenes: current.scenes.map((item) => item.scene === scene.scene ? { ...item, imageUrl: data.imageUrl } : item) } : current); } setStoryboard((current) => current ? { ...current, scenes } : current); setStatus("All scene visuals are ready. Render the Reel next."); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }

  async function renderReel() { if (!storyboard || storyboard.scenes.some((scene) => !scene.imageUrl)) return; setBusy("render"); setError(""); setVideoUrl(""); setSavedVideoUrl(""); setScheduled(false); setStatus("Generating natural voice narration…"); try { const narration = storyboard.scenes.map((scene) => scene.narration).join(" "); const audioResponse = await fetch("/api/reel-lab/tts", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text: narration, speaker: "luna" }) }); if (!audioResponse.ok) { const data: any = await audioResponse.json().catch(() => ({})); throw new Error(data.details || data.error || "Voice generation failed"); } const audioUrl = URL.createObjectURL(await audioResponse.blob()); const audio = new Audio(audioUrl); audio.preload = "auto"; await new Promise<void>((resolve, reject) => { audio.onloadedmetadata = () => resolve(); audio.onerror = () => reject(new Error("Generated voice could not be decoded.")); }); const canvas = canvasRef.current; if (!canvas) throw new Error("Canvas is unavailable."); const width = 540; const height = 960; canvas.width = width; canvas.height = height; const ctx = canvas.getContext("2d"); if (!ctx) throw new Error("Canvas rendering context is unavailable."); const [images, logo] = await Promise.all([Promise.all(storyboard.scenes.map((scene) => loadImage(scene.imageUrl as string))), loadImage(LOGOS[pageName])]); const canvasStream = canvas.captureStream(30); let audioStream: MediaStream; const capture = (audio as HTMLAudioElement & { captureStream?: () => MediaStream }).captureStream; if (capture) audioStream = capture.call(audio); else { const audioContext = new AudioContext(); const source = audioContext.createMediaElementSource(audio); const destination = audioContext.createMediaStreamDestination(); source.connect(destination); source.connect(audioContext.destination); audioStream = destination.stream; } const combined = new MediaStream([...canvasStream.getVideoTracks(), ...audioStream.getAudioTracks()]); const mimeCandidates = ["video/mp4;codecs=avc1.424028,mp4a.40.2", "video/mp4", "video/webm;codecs=vp9,opus", "video/webm;codecs=vp8,opus", "video/webm"]; const mimeType = mimeCandidates.find((candidate) => MediaRecorder.isTypeSupported(candidate)); if (!mimeType) throw new Error("This browser cannot record a supported Reel format. Try current Chrome or Safari."); const recorder = new MediaRecorder(combined, { mimeType, videoBitsPerSecond: 2500000, audioBitsPerSecond: 128000 }); const chunks: Blob[] = []; recorder.ondataavailable = (event) => { if (event.data.size) chunks.push(event.data); }; const done = new Promise<void>((resolve, reject) => { recorder.onstop = () => resolve(); recorder.onerror = () => reject(new Error("Browser video recording failed.")); }); recorder.start(250); await audio.play(); const starts: number[] = []; let cursor = 0; storyboard.scenes.forEach((scene) => { starts.push(cursor); cursor += scene.durationSeconds; }); const duration = Math.max(cursor, Number.isFinite(audio.duration) ? audio.duration + 0.2 : cursor); const started = performance.now(); const draw = () => { const elapsed = (performance.now() - started) / 1000; let index = storyboard.scenes.findIndex((scene, i) => elapsed >= starts[i] && elapsed < starts[i] + scene.durationSeconds); if (index < 0) index = storyboard.scenes.length - 1; const scene = storyboard.scenes[index]; const progress = Math.max(0, Math.min(1, (elapsed - starts[index]) / scene.durationSeconds)); ctx.fillStyle = "#111"; ctx.fillRect(0, 0, width, height); drawCover(ctx, images[index], width, height, progress); const gradient = ctx.createLinearGradient(0, height * 0.48, 0, height); gradient.addColorStop(0, "rgba(0,0,0,0)"); gradient.addColorStop(.48, "rgba(0,0,0,.18)"); gradient.addColorStop(1, "rgba(0,0,0,.88)"); ctx.fillStyle = gradient; ctx.fillRect(0, 0, width, height); drawBrandMark(ctx, logo, width); ctx.fillStyle = "rgba(255,255,255,.82)"; ctx.font = "700 13px system-ui, sans-serif"; ctx.fillText(`${index + 1}/5`, 24, 42); ctx.font = "700 27px system-ui, sans-serif"; const lines = wrapText(ctx, scene.caption, width - 48); const lineHeight = 35; const boxH = lines.length * lineHeight + 30; const boxY = height - boxH - 36; ctx.fillStyle = "rgba(0,0,0,.34)"; ctx.roundRect(18, boxY - 12, width - 36, boxH, 18); ctx.fill(); ctx.fillStyle = "#fff"; lines.forEach((line, lineIndex) => ctx.fillText(line, 32, boxY + 20 + lineIndex * lineHeight)); if (elapsed < duration) requestAnimationFrame(draw); else { audio.pause(); recorder.stop(); combined.getTracks().forEach((track) => track.stop()); } }; requestAnimationFrame(draw); await done; const output = new Blob(chunks, { type: mimeType }); setVideoUrl(URL.createObjectURL(output)); setVideoMime(mimeType); setStatus(`Reel render complete · ${Math.round(duration)}s · ${mimeType.startsWith("video/mp4") ? "MP4" : "WebM fallback"}.`); URL.revokeObjectURL(audioUrl); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }

  async function saveReelToR2() { if (!videoUrl || !videoMime.startsWith("video/mp4")) { setError("Production save requires an MP4 Reel. This browser produced WebM fallback."); return; } setBusy("save"); setError(""); try { const blob = await fetch(videoUrl).then((r) => r.blob()); const form = new FormData(); form.set("pageName", pageName); form.set("file", new File([blob], `${pageName.toLowerCase()}-reel.mp4`, { type: "video/mp4" })); const response = await fetch("/api/reel-lab/assets", { method: "POST", body: form }); const data: any = await response.json(); if (!response.ok || !data.videoUrl) throw new Error(data.details || data.error || "Could not save Reel to R2."); setSavedVideoUrl(data.videoUrl); setScheduled(false); setStatus("Production MP4 saved to R2. Ready to publish or schedule."); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }
  async function publishFacebookReel() { if (!savedVideoUrl || !storyboard) return; if (!window.confirm(`Publish this Reel now to the ${pageName} Facebook Page?`)) return; setBusy("publish"); setError(""); try { const response = await fetch("/api/autoposter/reels/publish", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pageName, videoUrl: savedVideoUrl, title: storyboard.title, caption: storyboard.scenes.map((scene) => scene.caption).join(" ") }) }); const data: any = await response.json(); if (!response.ok || !data.ok) throw new Error(data.details || data.error || "Facebook Reel publishing failed."); setStatus(`Facebook Reel published · video ${data.videoId}`); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }
  async function scheduleFacebookReel() { if (!savedVideoUrl || !storyboard || !scheduleAt) { setError("Choose a future date and time before scheduling."); return; } const timestamp = new Date(scheduleAt); if (!Number.isFinite(timestamp.getTime()) || timestamp.getTime() <= Date.now()) { setError("Schedule time must be in the future."); return; } if (!window.confirm(`Schedule this Reel for ${timestamp.toLocaleString()} on the ${pageName} Facebook Page?`)) return; setBusy("schedule"); setError(""); try { const response = await fetch("/api/autoposter/schedules/reel", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ pageName, videoUrl: savedVideoUrl, title: storyboard.title, caption: storyboard.scenes.map((scene) => scene.caption).join(" "), startAt: timestamp.toISOString() }) }); const data: any = await response.json(); if (!response.ok || !data.ok) throw new Error(data.details || data.error || "Facebook Reel scheduling failed."); setScheduled(true); setStatus(`Reel scheduled for ${timestamp.toLocaleString()} · ${pageName}`); } catch (e) { setError(e instanceof Error ? e.message : String(e)); } finally { setBusy(null); } }

  const readyForRender = !!storyboard && storyboard.scenes.length === 5 && storyboard.scenes.every((scene) => !!scene.imageUrl); const minSchedule = new Date(Date.now() + 60000).toISOString().slice(0, 16); const extension = videoMime.startsWith("video/mp4") ? "mp4" : "webm";
  return <main className="reel-lab-shell">
    <header className="reel-lab-header"><button className="reel-lab-back" onClick={() => { window.location.href = "/"; }}><ArrowLeft size={17} /> Studio</button><div><div className="reel-lab-kicker"><FilmStrip size={14} /> REEL LAB · TEST + PUBLISH</div><h1>Build one excellent educational Reel.</h1><p>Test the creative pipeline, then save an MP4 for Facebook publishing or scheduling.</p></div><div className="reel-lab-badge">LAB</div></header>
    <section className="reel-lab-grid">
      <div className="reel-lab-panel"><div className="reel-lab-section"><span>01</span><div><strong>Creative brief</strong><small>Fixed test topic · change language/page only</small></div></div><label>Page<select value={pageName} onChange={(e) => setPageName(e.target.value as "Zawaago" | "InnoTech")}><option>Zawaago</option><option>InnoTech</option></select></label><label>Language<select value={language} onChange={(e) => setLanguage(e.target.value)}><option>Hinglish</option><option>English</option><option>Hindi</option></select></label><div className="reel-topic"><span>Test topic</span><strong>{TOPIC}</strong></div><button className="reel-lab-primary" disabled={!!busy} onClick={createStoryboard}>{busy === "storyboard" ? <SpinnerGap className="spin" /> : <Sparkle />} {storyboard ? "Regenerate storyboard" : "Create storyboard"}</button></div>
      <div className="reel-lab-panel"><div className="reel-lab-section"><span>02</span><div><strong>Storyboard</strong><small>{storyboard ? `${storyboard.scenes.length} scenes · ${total.toFixed(0)}s` : "Waiting for the AI story"}</small></div></div>{!storyboard ? <div className="reel-empty"><FilmStrip size={30} /><span>Generate the storyboard to see the five-scene structure.</span></div> : <div className="scene-list">{storyboard.scenes.map((scene) => <article className="scene-card" key={scene.scene}><div className="scene-number">{scene.scene}</div><div><strong>{scene.caption}</strong><p>{scene.narration}</p><small>{scene.durationSeconds}s · visual plan ready</small></div></article>)}</div>}<button className="reel-lab-secondary" disabled={!storyboard || !!busy} onClick={generateSceneAssets}>{busy === "assets" ? <SpinnerGap className="spin" /> : <Sparkle />} Generate 5 scene visuals</button></div>
      <div className="reel-lab-panel reel-render-panel"><div className="reel-lab-section"><span>03</span><div><strong>Voice + render</strong><small>Cloudflare Aura-2 + browser compositor</small></div></div><div className="render-checks"><span className={readyForRender ? "ok" : ""}>{readyForRender ? <CheckCircle /> : <WarningCircle />} Scene visuals</span><span><SpeakerHigh /> Aura-2 narration</span><span><FilmStrip /> MP4 preferred · WebM fallback</span></div><canvas ref={canvasRef} className="reel-hidden-canvas" /><button className="reel-lab-primary" disabled={!readyForRender || !!busy} onClick={renderReel}>{busy === "render" ? <SpinnerGap className="spin" /> : <Play weight="fill" />} Render Reel</button>
        {videoUrl && <div className="video-result"><video controls playsInline src={videoUrl} /><a href={videoUrl} download={`zawaago-reel-lab-test.${extension}`}>Save test Reel</a>{videoMime.startsWith("video/mp4") && <button className="reel-lab-secondary" disabled={!!busy || !!savedVideoUrl} onClick={saveReelToR2}>{busy === "save" ? <SpinnerGap className="spin" /> : <Sparkle />} {savedVideoUrl ? "Saved to R2" : "Save MP4 to R2"}</button>}{savedVideoUrl && <div className="reel-publish-tools"><button className="reel-lab-primary" disabled={!!busy} onClick={publishFacebookReel}>{busy === "publish" ? <SpinnerGap className="spin" /> : <Play weight="fill" />} Publish Reel to {pageName}</button><div className="reel-schedule-inline"><label>Schedule date & time<input type="datetime-local" value={scheduleAt} min={minSchedule} onChange={(e) => { setScheduleAt(e.target.value); setScheduled(false); }} /></label><button className="reel-lab-secondary" disabled={!!busy || scheduled || !scheduleAt} onClick={scheduleFacebookReel}>{busy === "schedule" ? <SpinnerGap className="spin" /> : <CheckCircle />} {scheduled ? "Reel scheduled" : "Schedule Reel"}</button></div>{scheduled && <a className="reel-schedule-link" href="/schedule">View Reel schedule queue →</a>}</div>}</div>}
      </div></div>
    </section>
    {(status || error) && <div className={`reel-lab-status ${error ? "error" : ""}`}>{error ? <WarningCircle /> : <CheckCircle />}<span>{error || status}</span></div>}
    <footer className="reel-lab-footer">Testing target: <strong>premium faceless educational Reel</strong> · MP4 production path + Facebook publisher + scheduler integrated; final real-world Reel test still required.</footer>
  </main>;
}
